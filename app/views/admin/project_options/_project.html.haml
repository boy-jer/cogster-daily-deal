%tr
  %td=project.business.name
  %td=project.name
  %td=project.created_at.strftime("%b %d")
  %td=project.expiration_date.strftime("%b %d")
  %td=number_to_currency(project.goal)
  %td=project.percent_funded
