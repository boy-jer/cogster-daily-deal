-content_for :menu do
  =menu
#content
  .main.narrow
    %h2 Edit Community

    =form_for @community, :url => admin_community_path(@community) do |f|
      =render :partial => 'form', :object => f
    =link_to "Cancel", admin_communities_path
