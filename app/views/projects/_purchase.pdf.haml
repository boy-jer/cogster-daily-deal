%tr{ :class => cycle('odd', 'even')}
  %td=purchase.user.abbr_name
  %td=purchase.user.cogster_id
  -purchase.coupons.each do |coupon|
    %td=status_for(coupon)
  %td=purchase.created_at.strftime("%B %d")
  %td=number_to_currency(purchase.amount)
